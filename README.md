# NicheDocs-AI-
A web app where a user uploads a document (e.g. a student handbook, a labor-law FAQ, an internal policy doc) and asks natural-language questions about it. The app answers using only the document's content and shows exactly which page/section the answer came from, instead of relying on the model's general knowledge.
